#Virtualbox manual start, when defining shared folder in settings need use Devices 

-> Install Guest Addition
then -> sudo mount -t vboxsf share /home/shared

#Vagrant

Install vagrant
Install Virtualbox

vagrant init

vm box into C:\Users\Administrator\.vagrant.d\boxes

192.168.1.204