# vagrantsetup

vagrant init - Initialise vagrant on the system.

Virtual box will need to be installed.

Vagrant file will need to be changed to include a different box other than the 'chad-thompson-VAGRANTSLASH-ubuntu-trusty64-gui'. Local boxes should be located in 'C:\Users\[USER]\.vagrant.d\boxes'.

vagrant up - Creates machine.
